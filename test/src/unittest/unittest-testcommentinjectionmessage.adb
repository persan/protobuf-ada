-- Generated by the protocol buffer compiler.  DO NOT EDIT!
-- source: unittest.proto

pragma Ada_2012;

package body Unittest.TestCommentInjectionMessage is
  ---------------------------------------------------------------------------
  -- Inherited functions and procedures from Protocol_Buffers.Message -------
  ---------------------------------------------------------------------------

  procedure Clear
    (The_Message : in out Unittest.TestCommentInjectionMessage.Instance) is
  begin
    if (The_Message.Has_Bits (0 / 32) and Protocol_Buffers.Wire_Format.Shift_Left (16#FF#, 0 mod 32)) /= 0 then
      if The_Message.Has_A then
        The_Message.Clear_Has_A;
        declare
          procedure Free is new Ada.Unchecked_Deallocation (Protocol_Buffers.Wire_Format.PB_String, Protocol_Buffers.Wire_Format.PB_String_Access);
        begin
          if The_Message.A /= Default_A'Access then
            Free (The_Message.A);
          end if;
        end;
        The_Message.A := Default_A'Access;
      end if;
    end if;
    The_Message.Has_Bits := (others => 0);
  end Clear;

  procedure Copy
    (To   : in out Unittest.TestCommentInjectionMessage.Instance;
     From : in Unittest.TestCommentInjectionMessage.Instance) is
  begin
    To.Clear;
    To.Merge (From);
  end Copy;

  function Get_Type_Name
    (The_Message : in Unittest.TestCommentInjectionMessage.Instance) return Protocol_Buffers.Wire_Format.PB_String is
  begin
    return "protobuf_unittest.TestCommentInjectionMessage";
  end Get_Type_Name;

  function Is_Initialized
    (The_Message : in Unittest.TestCommentInjectionMessage.Instance) return Boolean is
  begin
    return True;
  end Is_Initialized;

  procedure Merge
    (To   : in out Unittest.TestCommentInjectionMessage.Instance;
     From : in Unittest.TestCommentInjectionMessage.Instance) is
  begin
    if (From.Has_Bits (0 / 32) and Protocol_Buffers.Wire_Format.Shift_Left (16#FF#, 0 mod 32)) /= 0 then
      -- optional string a = 1 [default = "*/ <- Neither should this."];
      if From.Has_A then
        To.Set_A(From.Get_A);
      end if;
    end if;
  end Merge;

  function Byte_Size
    (The_Message : in out Unittest.TestCommentInjectionMessage.Instance) return Protocol_Buffers.Wire_Format.PB_Object_Size is
    Total_Size : Protocol_Buffers.Wire_Format.PB_Object_Size := 0;
  begin
    if (The_Message.Has_Bits (0 / 32) and Protocol_Buffers.Wire_Format.Shift_Left (16#FF#, 0 mod 32)) /= 0 then
      -- optional string a = 1 [default = "*/ <- Neither should this."];
      if The_Message.Has_A then
        Total_Size := Total_Size + 1 + Protocol_Buffers.IO.Coded_Output_Stream.Compute_String_Size_No_Tag (The_Message.A.all);
      end if;
    end if;
    The_Message.Cached_Size := Total_Size;
    return Total_Size;
  end Byte_Size;

  procedure Serialize_With_Cached_Sizes
    (The_Message   : in Unittest.TestCommentInjectionMessage.Instance;
     The_Coded_Output_Stream : in
       Protocol_Buffers.IO.Coded_Output_Stream.Instance) is
  begin
    -- optional string a = 1 [default = "*/ <- Neither should this."];
    if The_Message.Has_A then
      The_Coded_Output_Stream.Write_String (1, The_Message.A.all);
    end if;
  end Serialize_With_Cached_Sizes;

  procedure Merge_Partial_From_Coded_Input_Stream
    (The_Message   : in out Unittest.TestCommentInjectionMessage.Instance;
     The_Coded_Input_Stream : in out
       Protocol_Buffers.IO.Coded_Input_Stream.Instance) is
    Tag : Protocol_Buffers.Wire_Format.PB_UInt32;
  begin
    Tag := The_Coded_Input_Stream.Read_Tag;
    while Tag /= 0 loop
      case Protocol_Buffers.Wire_Format.Get_Tag_Field_Number (Tag) is
      -- optional string a = 1 [default = "*/ <- Neither should this."];
      when 1 =>
        if Protocol_Buffers.Wire_Format.Get_Tag_Wire_Type (Tag) =
          Protocol_Buffers.Wire_Format.LENGTH_DELIMITED then
          declare
            procedure Free is new Ada.Unchecked_Deallocation (Protocol_Buffers.Wire_Format.PB_String, Protocol_Buffers.Wire_Format.PB_String_Access);
          begin
            if The_Message.A /= Default_A'Access then
              Free (The_Message.A);
            end if;
          end;
          The_Message.Set_Has_A;
          The_Message.A := The_Coded_Input_Stream.Read_String;
        end if;
      when others =>
        declare
          Dummy : Protocol_Buffers.Wire_Format.PB_Bool;
          pragma Unreferenced (Dummy);
        begin
          Dummy := The_Coded_Input_Stream.Skip_Field (Tag);
          return;
        end;
      end case;
      Tag := The_Coded_Input_Stream.Read_Tag;
    end loop;
  end Merge_Partial_From_Coded_Input_Stream;

  function Get_Cached_Size
    (The_Message : in Unittest.TestCommentInjectionMessage.Instance) return Protocol_Buffers.Wire_Format.PB_Object_Size is
  begin
    return The_Message.Cached_Size;
  end Get_Cached_Size;

  overriding
  procedure Finalize
    (The_Message : in out Unittest.TestCommentInjectionMessage.Instance) is
  begin
    declare
      procedure Free is new Ada.Unchecked_Deallocation (Protocol_Buffers.Wire_Format.PB_String, Protocol_Buffers.Wire_Format.PB_String_Access);
    begin
      if The_Message.A /= Default_A'Access then
        Free (The_Message.A);
      end if;
    end;
  end Finalize;

  ---------------------------------------------------------------------------
  -- Field accessor definitions ---------------------------------------------
  ---------------------------------------------------------------------------

  -- optional string a = 1 [default = "*/ <- Neither should this."];
  function Has_A
    (The_Message : in TestCommentInjectionMessage.Instance) return Boolean is
  begin
    return (The_Message.Has_Bits(0) and 16#00000001#) /= 0;
  end Has_A;

  procedure Set_Has_A
    (The_Message : in out TestCommentInjectionMessage.Instance) is
  begin
    The_Message.Has_Bits(0) := The_Message.Has_Bits(0) or 16#00000001#;
  end Set_Has_A;

  procedure Clear_Has_A
    (The_Message : in out TestCommentInjectionMessage.Instance) is
  begin
    The_Message.Has_Bits(0) := The_Message.Has_Bits(0) and (not 16#00000001#);
  end Clear_Has_A;

  procedure Clear_A
    (The_Message : in out TestCommentInjectionMessage.Instance) is
  begin
    The_Message.Clear_Has_A;
    declare
      procedure Free is new Ada.Unchecked_Deallocation (Protocol_Buffers.Wire_Format.PB_String, Protocol_Buffers.Wire_Format.PB_String_Access);
    begin
      if The_Message.A /= Default_A'Access then
        Free (The_Message.A);
      end if;
    end;
    The_Message.A := Default_A'Access;
    The_Message.Clear_Has_A;
  end Clear_A;

  function Get_A
    (The_Message : in TestCommentInjectionMessage.Instance) return Protocol_Buffers.Wire_Format.PB_String is
  begin
    return The_Message.A.all;
  end Get_A;

  function Get_A
    (The_Message : in out TestCommentInjectionMessage.Instance;
     Size : in Integer := -1) return Protocol_Buffers.Wire_Format.PB_String_Access is
  begin
    The_Message.Set_Has_A;
    if Size >= 0 then
      declare
        procedure Free is new Ada.Unchecked_Deallocation (Protocol_Buffers.Wire_Format.PB_String, Protocol_Buffers.Wire_Format.PB_String_Access);
      begin
        if The_Message.A /= Default_A'Access then
          Free (The_Message.A);
        end if;
      end;
      The_Message.A := new Protocol_Buffers.Wire_Format.PB_String'(1 .. Size => Character'Val (0));
      return The_Message.A;
    end if;

    if The_Message.A = Default_A'Access then
      The_Message.A := new String'(Default_A);
    end if;
    return The_Message.A;
  end Get_A;

  procedure Set_A
    (The_Message : in out TestCommentInjectionMessage.Instance;
     Value : in Protocol_Buffers.Wire_Format.PB_String) is
  begin
    The_Message.Set_Has_A;
    if The_Message.A /= Default_A'Access and then Value'Length = The_Message.A.all'Length then
      The_Message.A.all := Value;
    else
      declare
        procedure Free is new Ada.Unchecked_Deallocation (Protocol_Buffers.Wire_Format.PB_String, Protocol_Buffers.Wire_Format.PB_String_Access);
      begin
        if The_Message.A /= Default_A'Access then
          Free (The_Message.A);
        end if;
      end;
      The_Message.A := new Protocol_Buffers.Wire_Format.PB_String'(Value);
    end if;
  end Set_A;

  function Release_A
    (The_Message : in out TestCommentInjectionMessage.Instance) return Protocol_Buffers.Wire_Format.PB_String_Access is
  begin
    The_Message.Clear_Has_A;
    if The_Message.A = Default_A'Access then
      return null;
    else
      declare
        Temp : Protocol_Buffers.Wire_Format.PB_String_Access := The_Message.A;
      begin
        The_Message.A := Default_A'Access;
        return Temp;
      end;
    end if;
  end Release_A;

end Unittest.TestCommentInjectionMessage;
